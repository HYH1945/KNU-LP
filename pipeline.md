
1. 전체 시스템 구조 및 파일 구성
• web_app.py: Streamlit 기반 웹 인터페이스, 파일 업로드 및 사용자 정의 HR 해상도 설정
• processor/: 이미지 파일 검증, 해상도 비교, 조건별 분기 및 데이터 포맷팅 담당 • __init__.py • image_processor.py: 사용자 입력 해상도 기반 체크, 5장 초과 시 정렬/선택, 텐서 변환, 플레이스홀더 생성
• superresolution/: 고해상도 복원 모델 관련 모듈 • __init__.py • sr_model.py: SR module 로드 및 추론 제어
• ocr/: 문자 인식 및 결과 취합 관련 모듈 • __init__.py • ocr_model.py: 외부 OCR 모델 로드 및 단일 추론 • ensemble.py: 3장 이상 인풋 발생 시 OCR 결과들을 모아 멀티보팅 처리
2. 사용자 입력 및 전처리 분기 로직
• 사용자 입력: 웹 UI에서 기준이 되는 HR 해상도(가로, 세로)를 유동적으로 입력받음
• 5장 초과 시 처리: 업로드된 이미지가 5장을 초과하면 해상도가 높은 순으로 정렬하여 상위 5장만 선택
• 복원 모듈 진입 조건: 선택된 이미지들의 면적이 사용자가 입력한 HR 해상도 면적의 80% 이하일 때 SR module 호출
• 앙상블 모듈 진입 조건: 입력 이미지 면적이 기준의 80%를 초과(고해상도)하고, 번호판 이미지가 최소 3장 이상이면 SR module을 거치지 않고 바로 멀티보팅 OCR 처리
3. 핵심 모듈별 세부 동작 흐름
전처리 단계 (processor/image_processor.py)
• 업로드 이미지 RGB 변환 및 해상도 순 정렬
• 동적 해상도 분기 검사 (입력 면적과 사용자 정의 HR 면적의 80% 선 비교)
• SR module 진입용 데이터 포맷팅: 사용자가 입력한 HR 크기대로 첫 이미지를 Bicubic interpolation하여 placeholder HR 이미지 생성, LR 프레임은 규격에 맞게 리사이즈 및 텐서 변환
복원 단계 (superresolution/sr_model.py)
• 설정 파일을 통한 SR module 로드 및 eval 모드 전환
• placeholder HR과 전처리된 LR 프레임들을 묶어 배치 구성 후 초해상화 이미지 추론
OCR 및 후처리 단계 (ocr/)
• 단일 결과 처리: SR module을 거친 이미지 또는 단일 고해상도 이미지를 OCR 모델로 텍스트 추론
• 멀티보팅 처리: 3장 이상의 고해상도 이미지 입력 시, 각 프레임의 OCR 결과 중 가장 빈도수가 높은 글자를 조합하여 최종 판독할 것
